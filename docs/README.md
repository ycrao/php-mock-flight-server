php-mock-flight-server
----------------------

>   基于 `flightphp` 框架实现的 `mock` 服务器 。


## 初始化

```bash
composer create-project flightphp/skeleton php-mock-flight-server
```

## 修改

可以移除测试控制器，无关的路由配置等，这里我们只是注释掉。

